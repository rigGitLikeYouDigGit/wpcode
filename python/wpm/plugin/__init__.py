
"""might be a real bad idea to make this a subpackage
if we need to split it out, that's ok.

Direct children of plugin package are registered as individual plugins -
if they are packages, the __init__ file should define the plugin
register functions
"""
