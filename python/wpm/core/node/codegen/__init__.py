

"""tests for generating node wrappers procedurally
from maya's own nodes

separate process so it doesn't have to be run entirely within maya -
gather uniform data on node types into a "nodeData.json" file,
then go wild


"""