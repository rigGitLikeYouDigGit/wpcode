// more complete effects to insert into shaders

#ifndef SHADER_EFFECTS
#define SHADER_EFFECTS 1
#endif


vec3 screenPixels( vec3 colour, float patternWidth, float patternHeight,
    vec2 uv ){
    // creates discrete bars or dots, mimicking LED or LCD pixels
    // only bars for now
    vec3 result;

    return result;
}
